import { SpinnerContainer, SpinnerOverlay } from './spinners.styles';

const Spinner = () => {
  return (
    <SpinnerOverlay>
      <SpinnerContainer />
    </SpinnerOverlay>   
  );
};

export default Spinner;
